// This file will contain the providers for the Vetracoin app.
// Providers are used to manage global state and context for the app.

import React from 'react';

const Providers = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default Providers;
